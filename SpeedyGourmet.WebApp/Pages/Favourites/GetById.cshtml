@page
@model SpeedyGourmet.WebApp.Pages.Favourites.GetByIdModel
@{
}

<table class="table">
    <thead>
        <tr>
            <th>Favourite</th>
        </tr>
    </thead>

    <tbody>
        <tr>
            <td>@Model.favourite.User.Name</td>
        </tr>
        <tr>
            <td>@Model.favourite.Recipe.Title</td>
        </tr>
    </tbody>
</table>

<div class="d-flex justify-content-between">
    <a class="btn btn-danger" asp-area="" asp-page="/Favourites/GetAll"> Return <i class="bi bi-arrow-left"></i></a>
</div>
